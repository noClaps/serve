[package]
name = "serve"
version = "0.1.0"
edition = "2024"

[dependencies]
argparse = { git = "https://github.com/noClaps/argparse" }
axum = "0.8.9"
tokio = { version = "1.52.3", features = ["rt-multi-thread"] }
tower-http = { version = "0.6.11", features = ["fs"] }
